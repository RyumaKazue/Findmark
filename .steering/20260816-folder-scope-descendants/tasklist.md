# タスクリスト — folder-scope-descendants

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### タスクスキップが許可される唯一のケース
実装方針・アーキテクチャ・依存関係の変更により、タスク自体が技術的に不要/実行不能になった場合のみ。
スキップ時は `- [x] ~~タスク名~~（理由: 具体的な技術的理由）` の形式で記録する。

---

## フェーズ1: 索引にフォルダ ID のパスを持たせる

- [x] `SearchEntry` に `folderIdPath: string[]` を追加する
  - [x] `folderPath`（表示用の名前）と同じ長さ・同じ並びであることを doc コメントに書く
  - [x] 真のルートを含めない意味論（既存の `folderPath` と同じ）を維持する
- [x] `buildIndex` の `walk` で ID も積む（走査は増やさない）
- [x] 名前ではなく ID で持つ理由（同名フォルダの取り違え防止・フォルダ名の `/` 対応）をコメントに書く

## フェーズ2: スコープ判定の変更

- [x] `inScope` を「直下のみ」から「配下すべて」へ変更する（`folderIdPath.includes`）
- [x] doc コメントを実態へ更新する（旧「当該フォルダの直下のみ」）

## フェーズ3: ブラウズ時の並び

- [x] ブラウズ（クエリなし）用の比較関数を追加する
  - [x] スコープ指定時は「直下（`node.parentId === folderId`）→ それ以外」の2グループ
  - [x] 各グループ内は従来どおりタイトル昇順
  - [x] スコープ未指定（「すべて」）ではグループ分けしない
  - [x] **検索時には使わない**（関連度優先）ことをコメントで明示する
- [x] `search` のブラウズ経路をこの比較関数に差し替える
- [x] 検索経路（キーワードあり）は従来どおりであることを確認する

## フェーズ4: テスト

- [x] `SearchEngine.test.ts` のテストツリーにサブフォルダ階層を追加する（既存ケースを壊さない形で）
- [x] スコープ判定のテストを追加する
  - [x] 親フォルダ指定で直下 + サブフォルダ内が含まれる
  - [x] 孫フォルダ指定でその配下だけに絞られる
  - [x] 同名フォルダが別階層にあっても混ざらない（ID 判定の固定）
  - [x] スコープ未指定は全件
- [x] ブラウズ時の並びのテストを追加する
  - [x] 直下がサブフォルダ内より先
  - [x] 各グループ内はタイトル昇順
  - [x] 孫とひ孫は深さで区別されない（2段階の固定）
  - [x] スコープ未指定ではグループ分けが起きない
- [x] 検索時はスコア順であることのテストを追加する（深い階層の強い一致が上）
- [x] 「直下のみ」を期待していた既存テストの期待値を更新する（変更理由をテスト名/コメントに残す）

## フェーズ5: 型・モデル・文言の追従

- [x] `packages/shared/lib/types/search.ts` の doc コメント（`SearchQuery.folderScope` / `FolderScope`）を改訂する
- [x] `packages/i18n/locales/{ja,en}/messages.json` の `popupMetaScopedBrowse` の文言を変更する
- [x] `pages/popup/src/components/resultMetaModel.ts` の doc コメントの表を追従させる

## フェーズ6: 品質チェックと修正

- [x] `pnpm test`（フォアグラウンド実行で exit code 0）
- [x] `pnpm lint`（`--continue` のため各パッケージの結果と最終 exit code を確認）
- [x] `pnpm type-check`（exit code 0）
- [x] `implementation-validator` サブエージェントによる品質検証を実施し、指摘を解消する

## フェーズ7: 検証（モード3）

> 全タスク`[x]`は「作りきった」だけ。ここで「想定通りか」を判定する。

- [x] requirements.md の AC-1〜AC-10 と実装を突き合わせ OK/NG を一覧化する
- [x] 手動確認の観点をチェックリストとして提示する
- [ ] ユーザーに検証結果を提示し、受け入れ承認（ゲート2）を取得する
  - NG があった場合は「検証ログ」に記録し、原因分類(A/B/C)に応じて戻る

## フェーズ8: ドキュメント更新・振り返り（モード4）

- [ ] `docs/product-requirements.md` 機能5 の受け入れ条件を改訂する（「直下のみ」→「配下すべて」）
- [ ] `docs/functional-design.md` の「デザイン/初期案からの変更点」#4 を再改訂し、右ペインの説明を更新する
- [ ] `docs/design/README.md` の右ペインの表示仕様・メタ行の記述を更新する
- [ ] 本ファイル下部に振り返りを記録する

---

## 検証ログ

> 検証→戻る（モード3）が発生するたびに追記する。発生しなければ「なし」。

（未実施）

---

## 実装後の振り返り

### 実装完了日
（未実施）

### 計画と実績の差分

（未実施）

### 検証の要約（モード3）

（未実施）

### 学んだこと

（未実施）

### 次回への改善提案

（未実施）
